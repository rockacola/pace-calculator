# 2026-05-23

## What I worked on

Ported the pace calculator from the Next.js/React source (`PaceCalculatorClient.tsx`) into the Vue 3 scaffold. Created all source files: utils, constants, composable, three sub-components, the main view, and the CSS design system.

## Decisions made

- Extracted all state and derived values into `usePaceCalculator` composable rather than keeping them in the view — keeps the view template-focused.
- Used Tailwind v4 `@theme` for color tokens (generates `text-ink`, `bg-paper`, etc.) plus `:root` aliases (`--ink`, `--paper`) for SVG fills and inline style bindings.
- Dropped the "back to lab" link since this is a standalone app with no parent navigation.

## What I learned

Tailwind v4 `@theme` puts color variables at CSS-variable scope (`--color-ink`), so a separate `:root` block is needed if you want shorthand names (`--ink`) for SVG attribute references.

## Problems / friction

Vue's `vue/attributes-order` rule requires `v-model` before `type` on inputs — fixed with `eslint --fix`.

## Next step

Run the dev server and visually verify the layout, then add unit tests for `paceUtils.ts`.

---

## Session 2

## What I worked on

Broke `HomeView.vue` into five panel components (`InputsPanel`, `AdjustmentsPanel`, `GoalPacePanel`, `EquivalentsTable`, `TrainingZonesPanel`). Moved `isGoalDist` and `showPace` to `paceUtils.ts` as shared pure functions. Moved pulse animation state into `GoalPacePanel` (where it belongs). Removed the grid background and switched theme to sky-100 based colors.

## Decisions made

- `isGoalDist(km, goalKm)` and `showPace(sec, unit)` are now pure functions in `paceUtils.ts` — composable no longer exposes `showPace`.
- Pulse animation state (`pulseActive`) lives in `GoalPacePanel`, watching its own `secPerKm` and `unit` props rather than being managed in the view.
- Sky palette: body = sky-100 (`#e0f2fe`), panels = sky-50, borders = sky-200/300, accent = sky-500, active states use accent instead of near-black.

## What I learned

In Vue `<script setup>`, `defineProps<T>()` return value doesn't need to be captured if props are only used in the template (Vue auto-exposes them).

## Next step

Add unit tests for `paceUtils.ts` (`predict`, `fmtPace`, `fmtTime`, `showPace`, `isGoalDist`).
