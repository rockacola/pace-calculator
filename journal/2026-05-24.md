# 2026-05-24

## What I worked on

- Added sky-100 → sky-200 gradient background (transition starts at 70%)
- Made race distance chips and equivalents panel unit-aware (km: 5K/10K/15K/HM/FM, mi: 3mi/5mi/10mi/HM/FM)
- Merged the two "Equivalent" panels (chart + table) into one `BasePanel`
- Changed `formatTime` to always show hours (`00:15:20` instead of `15:20`)

## Decisions made

- `KM_DISTANCES` and `MI_DISTANCES` share `half`/`marathon` IDs so selections persist across unit switches; km-only selections (e.g. 15K) reset to marathon on switch
- `EquivalentsTable` stripped of its own `BasePanel` and rendered as a raw grid; uses `-mx-5 -mb-4` negative margins to break out of the parent panel's padding

## Next step

- Review training zones panel for any remaining hardcoded km assumptions

---

## What I worked on

- Added inline SVG icons to brighten the UI: ruler + clock in InputsPanel labels, chevron toggle + thermometer + mountain in AdjustmentsPanel, zap/leaf on GoalPacePanel spectrum strip ends, and braces/check/alert icons in "How it works" headings

## Decisions made

- Used inline SVGs over a library (no new dependency); Lucide-stroke style for consistency
- Replaced the `+` toggle in AdjustmentsPanel with a proper chevron that rotates 180° instead of 45°, which reads more naturally as a collapse/expand affordance

## Next step

- Consider adding an icon to the BasePanel title slot if a panel-level icon feels needed
