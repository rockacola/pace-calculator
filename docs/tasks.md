# Implementation Tasks

- [x] Scaffold project (Vue 3 + Vite + Tailwind v4 + Pinia + Vitest)
- [x] Port pace calculator from Next.js source
  - [x] `paceUtils.ts` — `predict`, `fmtPace`, `fmtTime`
  - [x] `distances.ts` / `zones.ts` — constants
  - [x] `usePaceCalculator` composable — all reactive state and derived values
  - [x] `TimeInput.vue` — HH:MM:SS widget
  - [x] `PaceRibbon.vue` — animated pace bar
  - [x] `PaceCurve.vue` — SVG Riegel prediction curve
  - [x] `HomeView.vue` — full calculator layout (inputs, results, zones)
  - [x] CSS design system — tokens, component classes, animations
